# Suggested commands
- Format: `cargo fmt`
- Lint: `cargo clippy --all-targets --all-features`
- Tests: `cargo test`
- Run main example (solve random masked instance): `cargo run`
- Run random game generator binary: `cargo run --bin randomly_generate_game`
- Build release (if needed for solver speed): `cargo build --release`
